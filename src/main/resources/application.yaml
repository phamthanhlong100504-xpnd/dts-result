server:
  port: 8083

spring:
  application:
    name: dts-result

  datasource:
    url: jdbc:postgresql://localhost:5432/dts_result_db
    username: admin
    password: khoi0000
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-init-sql: "SET TimeZone = 'Asia/Ho_Chi_Minh'"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
